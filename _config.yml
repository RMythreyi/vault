title: The Vault # name of the site
author: Mythreyi Ramesh # name of site's author
email: mythreyiramesh7@u.northwestern.edu # email of site's author
url: https://rmythreyi.github.io/vault # root address of the site
baseurl: "" # subpath of the site, e.g. "/blog" (leave it blank "" if you're site shouldn't use a subpath)
description: > # description of the site (multiple lines allowed)
  A collection of useful code snippets to reuse.

permalink: /:slug.html

favicon: "favicon.ico" # name+extension of favicon (which must be put on the root folder)
# goat_counter: "yoursitename" # put your GoatCounter name if you want to use GoatCounter analytics

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"
  back_home_text: "Home" # customize text for homepage link in post layout
  date_format: "%Y-%m-%d" # customize how date is formatted
  show_description: true # show blog description in home page

sass:
  style: :compressed

markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge
